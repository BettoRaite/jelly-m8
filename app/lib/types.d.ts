export interface User {
  id: number;
  name: string;
  profileImageUrl: string;
  role: string;
  accessKey: string;
}
export interface Profile {
  id: number;
  name: string;
  profileImageUrl: string;
  bio: string;
}
